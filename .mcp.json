{
    "servers": {
        "sqlite": {
            "command": "python",
            "args": [
                "-m",
                "src.database.mcp_server"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "workingDir": "${workspaceFolder}"
        }
    }
}
